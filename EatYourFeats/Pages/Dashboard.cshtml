@page
@model DashboardModel
@{
    ViewData["Title"] = "Home page";
}
<!--
    Programmers: Ginny Ke
    Date created: 10/27/24
    Date modified: 11/10/24
    Purpose: UI file for Dashboard Page, contains New Game, Continue, and Logout Button. Logout button redirects to Splash Page

    Preconditions: User must be authenticated, `DashboardModel` should handle `Logout` functionality.
    Postconditions: User is logged out and redirected to Splash page.
    Error and Exceptions: None explicitly handled; server handles authentication and redirection errors.
    Side Effects: Logs user out, removes authentication cookie, redirects to Splash page.
    Invariants: `Logout` form triggers `OnPostLogoutAsync()` in `DashboardModel`.
    Other Faults: N/A.
-->
<div class="text-center">
    <h1 class="display-4">Welcome to Eat Your Feats!</h1>
    <a class="btn btn-primary" asp-page="/Todo">New Game</a>
    <h2></h2>
    <a class="btn btn-primary" asp-page="/ManageToDo">Continue</a>

    <form method="post" asp-page-handler="Logout">
        <button type="submit" class="btn btn-danger mt-3">Logout</button>
    </form>
</div>